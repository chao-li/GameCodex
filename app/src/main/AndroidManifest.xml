<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.clidev.gamecodex">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".StarterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/igdb_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/igdb_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".populargamescreen.view.PopularGamesActivity"></activity>
        <activity android:name=".gamedetailscreen.view.GameDetailsActivity" />
        <activity android:name=".launchscreen.view.LaunchActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>